version: "3.9"
services:
  postgres:
    image: postgres:16
    container_name: sortflow-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sortflow
      POSTGRES_USER: sortflow
      POSTGRES_PASSWORD: sortflow_pw
    ports:
      - "5432:5432"
    volumes:
      - sortflow_pgdata:/var/lib/postgresql/data

volumes:
  sortflow_pgdata:
